# Copy to .env.local for local development. Never commit .env.local.

# Server-only: where Next.js's dev proxy (next.config.js rewrites) forwards
# /api/* requests. Points at the local Flask dev server.
INTERNAL_API_BASE_URL=http://127.0.0.1:5000
